import os
from dotenv import load_dotenv

load_dotenv()  # Load environment variables from .env file

import anthropic

client = anthropic.Anthropic()  # This will automatically use the environment variable
# Rest of your code remains the same
